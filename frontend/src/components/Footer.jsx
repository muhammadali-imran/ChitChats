function Footer() {
  return (
    <footer className="p-5 border-t border-primary-lighter text-primary-text text-sm">
      <small>
        Data and UI stubs remain separated from theme styles
        for easier maintenance.
      </small>
    </footer>
  );
}

export default Footer;
